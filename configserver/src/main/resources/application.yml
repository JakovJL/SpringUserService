server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JakovJL/SpringUserService
          search-paths: config
          clone-on-start: true
eureka:
  client:
    enabled: false
    register-with-eureka: false